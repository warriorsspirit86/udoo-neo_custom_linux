#!/bin/sh
set -e
# Automatically added by dh_installinit/11.1.6ubuntu1
if [ -x "/etc/init.d/hostapd" ] && [ "$1" = remove ]; then
	invoke-rc.d hostapd stop || exit 1
fi
# End automatically added section
